// WARNING: This file was autogenerated by MBRS generator.
// Do not update it because if you run generator again you changes will be deleted

package {{ model.package }};

import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;
import org.springframework.http.ResponseEntity;

import ftn.backendservice.domain.entities.{{ model.class_name }};
import ftn.backendservice.services.{{ model.service_class_name }};


@RestController
@RequestMapping("/api/{{ model.class_variable }}")
@RequiredArgsConstructor
public class {{ model.class_name }}Controller {

    private final {{ model.service_class_name }} {{ model.service_name }};

    @GetMapping("{id}")
    public ResponseEntity<?> getById(@PathVariable Long id) {
        return ResponseEntity.ok({{ model.service_name }}.getById(id));
    }

    @GetMapping(value = "/all")
    public ResponseEntity<?> getAll() {
        return ResponseEntity.ok({{ model.service_name }}.getAll());
    }

    @PostMapping(value = "/create")
    public ResponseEntity<?> create(@RequestBody {{ model.class_name }} request) {
        return ResponseEntity.ok({{ model.service_name }}.save(request));
    }

    @PutMapping("/{id}")
    public ResponseEntity<?> update(@PathVariable Long id, @RequestBody {{ model.class_name }} updateRequest) {
        return ResponseEntity.ok({{ model.service_name }}.update(updateRequest));
    }

}
