<script setup>
</script>

<template>
    <div class="d-flex justify-content-center">
        <div>
            <h3>Čestitamo! Uspešno ste generisali vašu aplikaciju.</h3>
            <div class="contain card shadow bg-light">
                <div class="card-body">
                    {% for clas in model.classes %}
                    <div class="link"><b>&nbsp;{{ clas | class_label_converter }}&nbsp;</b><a href="/{{ clas.name[0].lower() + clas.name[1:] }}/table">/{{ clas.name[0].lower() + clas.name[1:] }}/table</a></div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</template>

<style scoped>
h3 {
    font-weight: 600;
    margin-top: 35px;
}

a {
    margin-left: 5px;
    border: 1px solid #0d6efd;
    padding: 1px 7px;
    border-radius: 3px;
    text-decoration: none;
    background-color: #0d6efd;
    color: white;
    font-weight: 600;
}

a:hover {
    background-color: #0d5fda;
    text-decoration: underline;
}

.link {
    margin: 20px 0;
    display: flex;
    justify-content: left;
}

.contain {
    margin-top: 50px;
}
</style>